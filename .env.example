# Supabase project credentials (Project Settings → API)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# Optional: point the app at a different backend (defaults to the hosted API)
EXPO_PUBLIC_API_URL=https://mec-eatz.onrender.com

# EAS build project id (optional for local dev)
# EAS_PROJECT_ID=
